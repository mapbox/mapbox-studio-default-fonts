{
  "name": "@mapbox/mapbox-studio-default-fonts",
  "version": "0.2.0",
  "description": "Default font library for Mapbox Studio.",
  "main": "index.js",
  "scripts": {
    "build": "node bin/generate-font-families.js"
  },
  "dependencies": {
    "mapnik": "^3.6.2"
  }
}
